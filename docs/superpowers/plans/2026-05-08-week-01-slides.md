# Week 1 reveal.js Slide Deck Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Build a reveal.js slide deck for ENG 6813 Week 1, host it on GitHub Pages, and add a reusable `slides:` frontmatter mechanism so `canvas_sync` embeds the deck in the Week 1 Canvas page when pushed (push itself is performed manually after live preview).

**Architecture:** A self-contained reveal.js deck under `slides/week-01/index.html`, loading reveal.js 5.x from `cdn.jsdelivr.net` and a custom theme at `assets/css/reveal-theme.css` that mirrors the site's typography and color tokens. The Python `canvas_sync` tool gains an optional `slides` slug on `Week`, a `slides_base_url` on `Config`, and a small splicer that inserts an iframe block into the page body before the first `<h2>` heading. Weeks without `slides:` get exactly the body they get today (additive change only).

**Tech Stack:** reveal.js 5.x (CDN), Jekyll/GitHub Pages, Python (`canvas_sync` package, `pytest`), HTML/CSS, the existing site fonts (Press Start 2P, VT323, Source Serif 4, Source Sans 3).

**Spec:** `docs/superpowers/specs/2026-05-08-week-01-slides-design.md`

---

## File Structure

**Files created:**
- `slides/week-01/index.html` — the deck (Task 7)
- `slides/week-01/images/ai-policy.png` — image asset (Task 5)
- `slides/week-01/images/smbc-prompt.png` — image asset (Task 5)
- `slides/week-01/images/xkcd-disclaimer.png` — image asset (Task 5)
- `assets/css/reveal-theme.css` — custom reveal.js theme (Task 6)

**Files modified:**
- `canvas_sync/config.py` — add `slides_base_url` (Task 1)
- `canvas_config.example.yaml` — document `slides_base_url` (Task 1)
- `canvas_sync/models.py` — add `slides` field + `slides_section_html` (Task 2)
- `canvas_sync/parser.py` — read `slides:` from frontmatter (Task 3)
- `canvas_sync/__main__.py` — add `_splice_slides`, wire into `_push_week` (Task 4)
- `tests/test_config.py` — test for `slides_base_url` default (Task 1)
- `tests/test_models.py` — tests for `slides` field + `slides_section_html` (Task 2)
- `tests/test_parser.py` — test for parsing `slides:` (Task 3)
- `tests/test_cli.py` — tests for `_splice_slides` (Task 4)
- `weeks/week-01.md` — add `slides: week-01` frontmatter (Task 8)

**Working directory for all commands:** `C:\Users\anast\Documents\GitHub\InterdisciplinaryTeaching`

**Conventions used:**
- Test commands use `pytest -v -k <test_name>` for targeted runs.
- Each task ends with a commit. Commit messages follow the existing repo style (lowercase prefix `feat:` / `test:` / `docs:` etc.).
- Reveal.js version pinned to **5.2.1** throughout. To bump later, change all four CDN URLs in `slides/week-01/index.html` together.

---

## Task 1: Add `slides_base_url` to `Config`

**Files:**
- Modify: `canvas_sync/config.py`
- Modify: `canvas_config.example.yaml`
- Test: `tests/test_config.py`

- [ ] **Step 1: Write the failing test**

Add to `tests/test_config.py`:

```python
def test_load_config_default_slides_base_url(tmp_path):
    config_file = tmp_path / "canvas_config.yaml"
    config_file.write_text(
        "api_url: https://webcourses.ucf.edu\n"
        "api_key: test-key-123\n"
        "course_id: 99999\n"
    )
    cfg = load_config(str(config_file))
    assert cfg.slides_base_url == "https://anastasiasalter.net/InterdisciplinaryTeaching/slides/"


def test_load_config_custom_slides_base_url(tmp_path):
    config_file = tmp_path / "canvas_config.yaml"
    config_file.write_text(
        "api_url: https://webcourses.ucf.edu\n"
        "api_key: test-key-123\n"
        "course_id: 99999\n"
        "slides_base_url: https://example.com/slides/\n"
    )
    cfg = load_config(str(config_file))
    assert cfg.slides_base_url == "https://example.com/slides/"
```

- [ ] **Step 2: Run tests to verify they fail**

```
pytest tests/test_config.py -v -k slides_base_url
```

Expected: both new tests FAIL with `AttributeError: 'Config' object has no attribute 'slides_base_url'`.

- [ ] **Step 3: Add the field to `Config` and `load_config`**

Replace the entire contents of `canvas_sync/config.py` with:

```python
# canvas_sync/config.py
from __future__ import annotations

from dataclasses import dataclass

import yaml

DEFAULT_SLIDES_BASE_URL = "https://anastasiasalter.net/InterdisciplinaryTeaching/slides/"


@dataclass
class Config:
    api_url: str
    api_key: str
    course_id: int
    course_start: str = ""
    slides_base_url: str = DEFAULT_SLIDES_BASE_URL


def load_config(path: str) -> Config:
    with open(path) as f:
        data = yaml.safe_load(f)
    return Config(
        api_url=data["api_url"],
        api_key=data["api_key"],
        course_id=data["course_id"],
        course_start=str(data.get("course_start", "")),
        slides_base_url=data.get("slides_base_url", DEFAULT_SLIDES_BASE_URL),
    )
```

- [ ] **Step 4: Run tests to verify they pass**

```
pytest tests/test_config.py -v
```

Expected: all tests PASS (the two new ones plus the three pre-existing).

- [ ] **Step 5: Document the option in the example YAML**

Replace the entire contents of `canvas_config.example.yaml` with:

```yaml
api_url: https://webcourses.ucf.edu
api_key: YOUR_API_KEY_HERE
course_id: 123456
# Optional: base URL where reveal.js slide decks are published.
# Used by the Week.slides_section_html() helper to build iframe URLs
# for any week with a `slides:` frontmatter field.
# slides_base_url: https://anastasiasalter.net/InterdisciplinaryTeaching/slides/
```

- [ ] **Step 6: Run the full test suite**

```
pytest -v
```

Expected: all tests PASS, no regressions.

- [ ] **Step 7: Commit**

```
git add canvas_sync/config.py canvas_config.example.yaml tests/test_config.py
git commit -m "feat: add slides_base_url to Config with default"
```

---

## Task 2: Add `slides` field and `slides_section_html` to `Week`

**Files:**
- Modify: `canvas_sync/models.py`
- Test: `tests/test_models.py`

- [ ] **Step 1: Write the failing tests**

Append to `tests/test_models.py`:

```python
def test_week_slides_default_none():
    w = Week(
        week=2,
        title="Learning Theories",
        starts="2026-05-18",
        body_markdown="## Readings",
        workshop=None,
        assignments=[],
        discussion=None,
    )
    assert w.slides is None
    assert w.slides_section_html("https://example.com/slides/") is None


def test_week_slides_section_html_renders_iframe():
    w = Week(
        week=1,
        title="Welcome",
        starts="2026-05-11",
        body_markdown="## Readings",
        workshop=None,
        assignments=[],
        discussion=None,
        slides="week-01",
    )
    html = w.slides_section_html("https://example.com/slides/")
    assert html is not None
    # Header
    assert "<h2>Slides</h2>" in html
    # Iframe with full URL
    assert 'src="https://example.com/slides/week-01/"' in html
    # Responsive wrapper inline styles (Canvas strips classes)
    assert "padding-bottom:56.25%" in html
    # Allowfullscreen and lazy loading
    assert "allowfullscreen" in html
    assert 'loading="lazy"' in html
    # Fallback link
    assert 'href="https://example.com/slides/week-01/"' in html
    assert "Open slides in a new tab" in html


def test_week_slides_section_html_handles_trailing_slash():
    """slides_base_url with or without trailing slash should both produce the same URL."""
    w_with_slug = Week(
        week=1,
        title="W",
        starts="2026-05-11",
        body_markdown="",
        workshop=None,
        assignments=[],
        discussion=None,
        slides="week-01",
    )
    with_slash = w_with_slug.slides_section_html("https://example.com/slides/")
    without_slash = w_with_slug.slides_section_html("https://example.com/slides")
    # Both should produce a URL ending in /slides/week-01/
    assert "/slides/week-01/" in with_slash
    assert "/slides/week-01/" in without_slash
    # And not /slides//week-01/ from the with-slash variant
    assert "/slides//week-01/" not in with_slash
```

- [ ] **Step 2: Run tests to verify they fail**

```
pytest tests/test_models.py -v -k slides
```

Expected: all three new tests FAIL — `Week.__init__()` does not accept a `slides` keyword argument, and `slides_section_html` is not defined.

- [ ] **Step 3: Add the field and method to `Week`**

In `canvas_sync/models.py`, modify the `Week` dataclass. Find this block (currently around lines 93-128):

```python
@dataclass
class Week:
    week: int
    title: str
    starts: str
    body_markdown: str
    workshop: Optional[Workshop]
    assignments: list[Assignment] = field(default_factory=list)
    discussion: Optional[Discussion] = None
```

Replace it with:

```python
@dataclass
class Week:
    week: int
    title: str
    starts: str
    body_markdown: str
    workshop: Optional[Workshop]
    assignments: list[Assignment] = field(default_factory=list)
    discussion: Optional[Discussion] = None
    slides: Optional[str] = None
```

Then, immediately before the `@property` decorated `file_key` (the last property in the class), add a new method:

```python
    def slides_section_html(self, base_url: str) -> Optional[str]:
        """Render the iframe block to embed the deck in a Canvas page.

        Returns None when no slide deck is configured for this week.
        Inline styles are used (not classes) because Canvas strips
        unknown CSS classes from page bodies.
        """
        if not self.slides:
            return None
        url = f"{base_url.rstrip('/')}/{self.slides}/"
        return (
            "<h2>Slides</h2>\n"
            '<div class="slides-embed" style="position:relative; '
            'padding-bottom:56.25%; height:0; overflow:hidden; max-width:100%;">\n'
            f'  <iframe src="{url}" '
            'style="position:absolute; top:0; left:0; width:100%; height:100%; border:0;" '
            'allowfullscreen loading="lazy" title="Week Slides"></iframe>\n'
            "</div>\n"
            f'<p><a href="{url}" target="_blank" rel="noopener">Open slides in a new tab</a></p>\n'
        )
```

- [ ] **Step 4: Run tests to verify they pass**

```
pytest tests/test_models.py -v
```

Expected: all tests PASS (eight pre-existing plus three new).

- [ ] **Step 5: Run the full test suite**

```
pytest -v
```

Expected: all tests PASS.

- [ ] **Step 6: Commit**

```
git add canvas_sync/models.py tests/test_models.py
git commit -m "feat: add optional slides slug and slides_section_html to Week"
```

---

## Task 3: Read `slides:` from frontmatter in the parser

**Files:**
- Modify: `canvas_sync/parser.py`
- Test: `tests/test_parser.py`

- [ ] **Step 1: Write the failing tests**

Append to `tests/test_parser.py`:

```python
SAMPLE_WITH_SLIDES = """---
week: 1
title: "Welcome and Interdisciplinary Teaching"
starts: 2026-05-11
slides: week-01
assignments:
  - title: "Activity Verification"
    points: 50
    due: 2026-05-15
---

## Readings

- Some reading
"""


def test_parse_week_file_with_slides(tmp_path):
    f = tmp_path / "week-01.md"
    f.write_text(SAMPLE_WITH_SLIDES)
    week = parse_week_file(str(f))
    assert week.slides == "week-01"


def test_parse_week_file_without_slides_defaults_none(tmp_path):
    f = tmp_path / "week-01.md"
    f.write_text(SAMPLE_WEEK_MD)  # SAMPLE_WEEK_MD does not contain a `slides:` field
    week = parse_week_file(str(f))
    assert week.slides is None
```

- [ ] **Step 2: Run tests to verify they fail**

```
pytest tests/test_parser.py -v -k slides
```

Expected: `test_parse_week_file_with_slides` FAILS because `Week.slides` defaults to `None` and the parser is not yet reading the field. `test_parse_week_file_without_slides_defaults_none` may pass already (since `slides` defaults to `None`), which is fine.

- [ ] **Step 3: Update the parser to read `slides:`**

In `canvas_sync/parser.py`, find the `Week(...)` constructor call at the end of `parse_week_file` (currently lines 59-67):

```python
    return Week(
        week=meta["week"],
        title=meta["title"],
        starts=str(meta["starts"]),
        body_markdown=post.content,
        workshop=workshop,
        assignments=assignments,
        discussion=discussion,
    )
```

Replace with:

```python
    return Week(
        week=meta["week"],
        title=meta["title"],
        starts=str(meta["starts"]),
        body_markdown=post.content,
        workshop=workshop,
        assignments=assignments,
        discussion=discussion,
        slides=meta.get("slides"),
    )
```

- [ ] **Step 4: Run tests to verify they pass**

```
pytest tests/test_parser.py -v
```

Expected: all tests PASS.

- [ ] **Step 5: Run the full test suite**

```
pytest -v
```

Expected: all tests PASS.

- [ ] **Step 6: Commit**

```
git add canvas_sync/parser.py tests/test_parser.py
git commit -m "feat: parse optional slides field from week frontmatter"
```

---

## Task 4: Add `_splice_slides` and integrate into `_push_week`

**Files:**
- Modify: `canvas_sync/__main__.py`
- Test: `tests/test_cli.py`

- [ ] **Step 1: Write the failing tests**

Read the current `tests/test_cli.py` first to see its style, then append:

```python
from canvas_sync.__main__ import _splice_slides


def test_splice_slides_inserts_before_first_h2():
    body = (
        "<p>Welcome paragraph.</p>\n"
        "<p>NEH workshop note.</p>\n"
        "<h2>Readings</h2>\n"
        "<ul><li>A reading</li></ul>\n"
        "<h2>Discussion Prompt</h2>\n"
        "<p>Question.</p>\n"
    )
    slides = "<h2>Slides</h2>\n<div>iframe-here</div>\n"
    result = _splice_slides(body, slides)
    # Slides block should appear in the output exactly once
    assert result.count("<h2>Slides</h2>") == 1
    # Original Readings heading still present
    assert "<h2>Readings</h2>" in result
    # Slides block should appear BEFORE Readings
    slides_idx = result.index("<h2>Slides</h2>")
    readings_idx = result.index("<h2>Readings</h2>")
    assert slides_idx < readings_idx
    # Welcome paragraph should still appear before slides
    welcome_idx = result.index("Welcome paragraph.")
    assert welcome_idx < slides_idx


def test_splice_slides_appends_when_no_h2():
    body = "<p>Just an intro paragraph, no headings.</p>\n"
    slides = "<h2>Slides</h2>\n<div>iframe</div>\n"
    result = _splice_slides(body, slides)
    # Body intact
    assert "Just an intro paragraph" in result
    # Slides block appended at the end
    assert result.endswith(slides) or result.rstrip().endswith(slides.rstrip())


def test_splice_slides_returns_body_unchanged_when_slides_html_empty():
    body = "<p>Hello</p>\n<h2>Readings</h2>\n"
    result = _splice_slides(body, "")
    assert result == body


def test_splice_slides_returns_body_unchanged_when_slides_html_none():
    body = "<p>Hello</p>\n<h2>Readings</h2>\n"
    result = _splice_slides(body, None)
    assert result == body
```

- [ ] **Step 2: Run tests to verify they fail**

```
pytest tests/test_cli.py -v -k splice
```

Expected: all four new tests FAIL with `ImportError: cannot import name '_splice_slides' from 'canvas_sync.__main__'`.

- [ ] **Step 3: Add the splicer function**

In `canvas_sync/__main__.py`, immediately before the `def _push_week(...)` function definition (currently around line 69), add:

```python
def _splice_slides(body_html: str, slides_html: Optional[str]) -> str:
    """Insert the slides iframe block into a rendered page body.

    Strategy: place the slides block immediately before the first `<h2>`
    heading. If no `<h2>` is present, append to the end. If `slides_html`
    is empty or None, return the body unchanged.
    """
    if not slides_html:
        return body_html
    idx = body_html.find("<h2")
    if idx == -1:
        return body_html.rstrip() + "\n" + slides_html
    return body_html[:idx] + slides_html + body_html[idx:]
```

`Optional` is already imported via `typing`? Check the existing imports at the top of `__main__.py`. If `Optional` is not imported, add `from typing import Optional` to the imports block.

- [ ] **Step 4: Wire `_splice_slides` into `_push_week`**

In `canvas_sync/__main__.py`, find the page-update block inside `_push_week` (around lines 100-108):

```python
        if page is not None:
            local_fields = {"title": week.module_name, "body": week.body_html}
            remote_fields = {"title": page.title, "body": page.body or ""}
            diffs = compute_diff(local_fields, remote_fields)
            if diffs:
                display_diff(f"Page \"{week.module_name}\"", diffs, console)
                if _confirm(f"Page \"{week.module_name}\"", apply_all_ref):
                    cs.update_page(week_state["page_url"], **local_fields)
                    console.print("  [green]Updated page[/green]")
```

Replace the `local_fields = {"title": week.module_name, "body": week.body_html}` line with:

```python
            slides_html = week.slides_section_html(cs.config.slides_base_url) if week.slides else None
            body_with_slides = _splice_slides(week.body_html, slides_html)
            local_fields = {"title": week.module_name, "body": body_with_slides}
```

Also find the page-create block (around lines 85-89):

```python
    # Page
    if "page_url" not in week_state:
        page_url = cs.create_page(week)
        week_state["page_url"] = page_url
        cs.add_module_item(module_id, "Page", page_url, week.module_name)
        console.print(f"  [green]Created page[/green] (url={page_url})")
```

The `cs.create_page(week)` method in `canvas_sync/canvas_api.py` currently uses `week.body_html` directly. We need it to splice in the slides too. The cleanest way is to pass the body in explicitly. Modify `cs.create_page` (in `canvas_sync/canvas_api.py`) to accept an optional body override:

```python
    def create_page(self, week, body_html: Optional[str] = None) -> str:
        page = self._course.create_page(
            wiki_page={
                "title": week.module_name,
                "body": body_html if body_html is not None else week.body_html,
                "published": False,
            }
        )
        return page.url
```

Add `from typing import Optional` to `canvas_api.py` if not already imported.

Then, in `__main__.py`, replace the create-page block with:

```python
    # Page
    if "page_url" not in week_state:
        slides_html = week.slides_section_html(cs.config.slides_base_url) if week.slides else None
        body_with_slides = _splice_slides(week.body_html, slides_html)
        page_url = cs.create_page(week, body_html=body_with_slides)
        week_state["page_url"] = page_url
        cs.add_module_item(module_id, "Page", page_url, week.module_name)
        console.print(f"  [green]Created page[/green] (url={page_url})")
```

- [ ] **Step 5: Expose `config` on `CanvasSync`**

The page-update block reads `cs.config.slides_base_url`. The current `CanvasSync.__init__` (in `canvas_sync/canvas_api.py`) stores config but does not expose it as a public attribute. Update its `__init__`:

Find:
```python
    def __init__(self, config: Config):
        self._canvas = Canvas(config.api_url, config.api_key)
        self._course = self._canvas.get_course(config.course_id)
```

Replace with:
```python
    def __init__(self, config: Config):
        self.config = config
        self._canvas = Canvas(config.api_url, config.api_key)
        self._course = self._canvas.get_course(config.course_id)
```

- [ ] **Step 6: Update the diff command path too**

The `cmd_diff` function (around lines 274-281 in `__main__.py`) compares local body to remote without splicing slides. Find:

```python
        if "page_url" in ws:
            page = cs.get_page(ws["page_url"])
            local_fields = {"title": week.module_name, "body": week.body_html}
            remote_fields = {"title": page.title, "body": page.body or ""}
```

Replace with:

```python
        if "page_url" in ws:
            page = cs.get_page(ws["page_url"])
            slides_html = week.slides_section_html(cs.config.slides_base_url) if week.slides else None
            body_with_slides = _splice_slides(week.body_html, slides_html)
            local_fields = {"title": week.module_name, "body": body_with_slides}
            remote_fields = {"title": page.title, "body": page.body or ""}
```

- [ ] **Step 7: Run targeted tests to verify they pass**

```
pytest tests/test_cli.py -v -k splice
```

Expected: all four new tests PASS.

- [ ] **Step 8: Run the full test suite**

```
pytest -v
```

Expected: all tests PASS. If any pre-existing CLI test now fails because of the `cs.config` change, inspect the failure — most likely the test mocks `CanvasSync` and the mock needs a `.config` attribute. Add it to the mock if so.

- [ ] **Step 9: Commit**

```
git add canvas_sync/__main__.py canvas_sync/canvas_api.py tests/test_cli.py
git commit -m "feat: splice slide-deck iframe into Canvas page body when slides set"
```

---

## Task 5: Copy the three images into `slides/week-01/images/`

**Files:**
- Create: `slides/week-01/images/ai-policy.png`
- Create: `slides/week-01/images/smbc-prompt.png`
- Create: `slides/week-01/images/xkcd-disclaimer.png`

- [ ] **Step 1: Create the directory and copy images**

PowerShell:

```powershell
New-Item -ItemType Directory -Path "slides\week-01\images" -Force | Out-Null
Copy-Item "additions\ai-policy.png" "slides\week-01\images\ai-policy.png"
Copy-Item "additions\smbc-prompt.png" "slides\week-01\images\smbc-prompt.png"
Copy-Item "additions\xkcd-disclaimer.png" "slides\week-01\images\xkcd-disclaimer.png"
```

- [ ] **Step 2: Verify the files exist and have non-zero size**

```
Get-ChildItem slides\week-01\images\
```

Expected: three PNG files listed, each > 100 KB.

- [ ] **Step 3: Commit**

```
git add slides/week-01/images/
git commit -m "feat: add Week 1 slide deck images"
```

---

## Task 6: Create the custom reveal.js theme

**Files:**
- Create: `assets/css/reveal-theme.css`

- [ ] **Step 1: Write the theme file**

Create `assets/css/reveal-theme.css` with the following contents:

```css
/* ============================================
   Custom reveal.js theme — Interdisciplinary Teaching
   Mirrors site fonts and color tokens from assets/css/style.css.
   Loaded after reveal.css. Drives off [data-theme] on <html>.
   ============================================ */

@import url('https://fonts.googleapis.com/css2?family=Press+Start+2P&family=VT323&family=Source+Serif+4:ital,opsz,wght@0,8..60,400;0,8..60,600;0,8..60,700;1,8..60,400&family=Source+Sans+3:wght@400;600;700&display=swap');

/* --- Light mode tokens (default) --- */
:root,
[data-theme="light"] {
  --bg: #f5f0e6;
  --bg-alt: #ebe4d4;
  --text: #2c2416;
  --text-muted: #6b5d4d;
  --heading: #3a2a14;
  --link: #5b6e28;
  --link-hover: #3d4a1a;
  --accent: #7a8b3c;
  --accent-soft: #c8d4a0;
  --border: #c9bfa8;
}

/* --- Dark mode tokens --- */
[data-theme="dark"] {
  --bg: #1a1a12;
  --bg-alt: #242418;
  --text: #c8c0a8;
  --text-muted: #8a8068;
  --heading: #d8d0b0;
  --link: #a0c040;
  --link-hover: #c8e060;
  --accent: #7a8b3c;
  --accent-soft: #3a4420;
  --border: #3e3a2c;
}

/* --- Base reveal surface --- */
html, body {
  background: var(--bg);
}

.reveal {
  font-family: 'Source Serif 4', Georgia, 'Times New Roman', serif;
  color: var(--text);
  background: var(--bg);
}

.reveal .slides {
  text-align: left;
}

.reveal .slides > section,
.reveal .slides > section > section {
  padding: 1.5em 1em;
}

/* --- Headings --- */
.reveal h1,
.reveal h2,
.reveal h3,
.reveal h4 {
  font-family: 'Press Start 2P', monospace;
  color: var(--heading);
  font-weight: 400;
  text-transform: none;
  line-height: 1.4;
  letter-spacing: 0.02em;
}

/* Press Start 2P is wide; reveal defaults overflow. Scale down. */
.reveal h1 { font-size: 1.4em; margin-bottom: 0.6em; }
.reveal h2 { font-size: 1.0em; margin-bottom: 0.5em; color: var(--accent); }
.reveal h3 { font-size: 0.8em; margin-bottom: 0.4em; color: var(--accent); }

/* Section header / transition slide */
.reveal section.section-header h1 {
  font-size: 1.6em;
  text-align: center;
}

/* Title slide */
.reveal section.title-slide {
  text-align: center;
}
.reveal section.title-slide h1 {
  text-align: center;
  font-size: 1.2em;
}
.reveal section.title-slide .subtitle {
  font-family: 'VT323', monospace;
  font-size: 1.6em;
  color: var(--text-muted);
  margin-top: 1em;
}

/* --- Body text --- */
.reveal p,
.reveal li {
  font-family: 'Source Serif 4', Georgia, serif;
  font-size: 0.7em;
  line-height: 1.5;
  color: var(--text);
}

.reveal ul, .reveal ol {
  margin-left: 1.5em;
}

.reveal li {
  margin-bottom: 0.4em;
}

.reveal strong { color: var(--heading); }

.reveal a {
  color: var(--link);
  text-decoration: underline;
}

.reveal a:hover {
  color: var(--link-hover);
}

/* --- Blockquote (used for long source quotes) --- */
.reveal blockquote {
  font-family: 'Source Serif 4', Georgia, serif;
  font-style: normal;
  font-size: 0.55em;
  line-height: 1.55;
  color: var(--text);
  background: var(--bg-alt);
  border-left: 4px solid var(--accent);
  padding: 1em 1.4em;
  margin: 0.5em 0;
  width: 90%;
  box-shadow: none;
  border-radius: 0 4px 4px 0;
}

.reveal blockquote .attribution {
  display: block;
  font-family: 'VT323', monospace;
  font-style: normal;
  font-size: 1.2em;
  color: var(--text-muted);
  margin-top: 0.8em;
}

/* --- Tables --- */
.reveal table {
  width: 100%;
  border-collapse: collapse;
  font-size: 0.55em;
  margin: 0.5em 0;
}
.reveal table th {
  background: var(--accent);
  color: #fff;
  font-family: 'Source Sans 3', sans-serif;
  text-align: left;
  padding: 0.5em 0.7em;
}
.reveal table td {
  padding: 0.5em 0.7em;
  border-bottom: 1px solid var(--border);
}
.reveal table tr:nth-child(even) td {
  background: var(--bg-alt);
}

/* --- Code --- */
.reveal code {
  font-family: 'VT323', 'Courier New', monospace;
  background: var(--bg-alt);
  padding: 0 0.3em;
  border-radius: 3px;
}

/* --- Images --- */
.reveal section img {
  max-height: 80vh;
  max-width: 90%;
  border: none;
  background: none;
  box-shadow: none;
  display: block;
  margin: 0 auto;
}

/* Webcomic slides get a small accent border */
.reveal section.webcomic img {
  border: 2px solid var(--accent);
  background: #fff;  /* xkcd/SMBC are line art on white */
  padding: 0.4em;
}

.reveal .image-caption {
  font-family: 'VT323', monospace;
  font-size: 1.0em;
  color: var(--text-muted);
  text-align: center;
  margin-top: 0.6em;
}

/* --- Reveal UI chrome --- */
.reveal .controls {
  color: var(--accent);
}
.reveal .progress {
  color: var(--accent);
  background: var(--bg-alt);
}
.reveal .slide-number {
  font-family: 'VT323', monospace;
  background: transparent;
  color: var(--text-muted);
}

/* --- In-deck dark-mode toggle button --- */
.deck-theme-toggle {
  position: fixed;
  top: 0.6em;
  right: 0.6em;
  z-index: 50;
  background: var(--bg-alt);
  color: var(--text);
  border: 2px solid var(--accent);
  border-radius: 4px;
  padding: 0.35em 0.55em;
  cursor: pointer;
  font-family: 'VT323', monospace;
  font-size: 1.1em;
  line-height: 1;
}
.deck-theme-toggle:hover {
  background: var(--accent);
  color: var(--bg);
}
[data-theme="light"] .deck-theme-toggle .dark-icon { display: none; }
[data-theme="dark"] .deck-theme-toggle .light-icon { display: none; }

/* --- Print fallback --- */
@media print {
  .deck-theme-toggle { display: none; }
}
```

- [ ] **Step 2: Sanity-check the file**

```
Get-Content assets\css\reveal-theme.css -TotalCount 5
```

Expected: file starts with the comment block.

- [ ] **Step 3: Commit**

```
git add assets/css/reveal-theme.css
git commit -m "feat: add custom reveal.js theme matching site typography and tokens"
```

---

## Task 7: Build the Week 1 slide deck

**Files:**
- Create: `slides/week-01/index.html`

This is the longest task — it creates the deck with all 31 reveal.js sections. There are no automated tests for slide content; verification is visual in Task 9.

- [ ] **Step 1: Create the deck file**

Write `slides/week-01/index.html` with the following contents. Use a single Write call — do not split into multiple edits.

```html
<!DOCTYPE html>
<html lang="en" data-theme="light">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>ENG 6813 — Week 1 Slides</title>

  <!-- reveal.js 5.2.1 (structural CSS only; cosmetic theme is our custom file) -->
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/reveal.js@5.2.1/dist/reveal.css" />

  <!-- Custom site-matching theme. Use a relative path so it works at /slides/week-01/. -->
  <link rel="stylesheet" href="../../assets/css/reveal-theme.css" />
</head>
<body>
  <button class="deck-theme-toggle" id="deck-theme-toggle" aria-label="Toggle dark mode" type="button">
    <span class="light-icon">&#9728;</span><span class="dark-icon">&#9789;</span>
  </button>

  <div class="reveal">
    <div class="slides">

      <!-- 1. TITLE -->
      <section class="title-slide">
        <h1>ENG 6813<br/>Interdisciplinary Teaching</h1>
        <p class="subtitle">Salter &amp; Stanfill &middot; Summer C 2026</p>
      </section>

      <!-- 2. COURSE DESCRIPTION -->
      <section>
        <h1>Course Description</h1>
        <p>Theory and practice of designing interdisciplinary courses and curricula for the humanities. For graduate students who plan to seek faculty positions or who want to strengthen their teaching practice.</p>
        <p>Engages foundational questions of pedagogy &mdash; how people learn, how to design effective assignments and assessments, how to build inclusive and student-centered courses &mdash; while addressing the challenges and opportunities of generative AI in the humanities classroom.</p>
      </section>

      <!-- 3. OVERVIEW -->
      <section>
        <h1>Over Twelve Weeks</h1>
        <ul>
          <li>Explore interdisciplinary humanities approaches to pedagogy.</li>
          <li>Design a course from the ground up (or redesign an existing one).</li>
          <li>Engage with agentic AI tools (Anthropic&rsquo;s Claude).</li>
          <li>Develop professional materials, including a teaching statement.</li>
        </ul>
      </section>

      <!-- 4. NEH WORKSHOP INTEGRATION -->
      <section>
        <h1>NEH Workshop Integration</h1>
        <p>Connected to the NEH project <em>Building a Digital Humanities Generative AI Learning Community</em>. Optional live workshops, biweekly at CHDR, 10 AM &ndash; noon:</p>
        <table>
          <thead>
            <tr><th>Date</th><th>Workshop</th></tr>
          </thead>
          <tbody>
            <tr><td>May 13</td><td>Introducing AI for DH Pedagogy</td></tr>
            <tr><td>May 27</td><td>AI for Textual Analysis</td></tr>
            <tr><td>June 10</td><td>AI for Visual Analysis</td></tr>
            <tr><td>June 24</td><td>Web and Interactive Applications</td></tr>
            <tr><td>July 8</td><td>Playful Approaches and Creative Code</td></tr>
            <tr><td>July 22</td><td>Agentic Futures, Curricular Sustainability</td></tr>
          </tbody>
        </table>
      </section>

      <!-- 5. COURSE OBJECTIVES -->
      <section>
        <h1>Course Objectives</h1>
        <ol>
          <li>Engage with humanities and DH approaches to pedagogy.</li>
          <li>Make informed decisions about teaching strategies, including AI policies.</li>
          <li>Compose a meaningful teaching statement.</li>
          <li>Design student-centered in-person and online courses.</li>
          <li>Explore best practices for accessibility and universal design.</li>
          <li>Critically evaluate generative AI tools in humanities pedagogy.</li>
          <li>Use generative AI tools (Claude Artifacts, Claude Code Web) to create and assess pedagogical materials.</li>
        </ol>
      </section>

      <!-- 6. TEXTS -->
      <section>
        <h1>Texts</h1>
        <p>No required textbook. Readings are open-access articles, blog posts, and PDFs. We draw heavily on practitioners. Consider following:</p>
        <ul>
          <li><strong>Ethan Mollick</strong> &mdash; <em>One Useful Thing</em> (AI in education)</li>
          <li><strong>Simon Willison</strong> &mdash; <em>simonwillison.net</em> (AI tools, vibe coding)</li>
          <li><strong>Dan Cohen</strong> &mdash; <em>Humane Ingenuity</em> (AI, DH, cultural heritage)</li>
          <li><strong>Ted Underwood</strong> &mdash; <em>The Stone and the Shell</em> (distant reading, AI)</li>
        </ul>
      </section>

      <!-- 7. ANTHROPIC SUBSCRIPTION -->
      <section>
        <h1>Anthropic Subscription</h1>
        <p>A paid subscription to <strong>Anthropic&rsquo;s Claude</strong> is required for hands-on exercises throughout the semester.</p>
        <ul>
          <li><strong>Claude Artifacts</strong> &mdash; used in the first half of the course.</li>
          <li><strong>Claude Code Web</strong> &mdash; used in the second half.</li>
        </ul>
        <p>A paid (Pro) subscription is needed to access the full functionality the course assignments rely on.</p>
      </section>

      <!-- 8. GRADING -->
      <section>
        <h1>Grading</h1>
        <table>
          <thead><tr><th>Pts</th><th>Assignment</th><th>Due</th></tr></thead>
          <tbody>
            <tr><td>50</td><td>Activity Verification</td><td>May 15</td></tr>
            <tr><td>240</td><td>Discussion Posts (8 &times; 30)</td><td>Weekly</td></tr>
            <tr><td>200</td><td>Signature Assignment</td><td>Week 8</td></tr>
            <tr><td>200</td><td>Course Syllabus</td><td>Week 10</td></tr>
            <tr><td>150</td><td>Teaching Statement</td><td>Week 11</td></tr>
            <tr><td>160</td><td>Final Portfolio &amp; Reflection</td><td>August 1</td></tr>
          </tbody>
        </table>
        <p>Total: 1000 points. Late work accepted without penalty for one week after the listed deadline.</p>
      </section>

      <!-- 9. SECTION HEADER: DH PEDAGOGY -->
      <section class="section-header">
        <h1>DH Pedagogy</h1>
      </section>

      <!-- 10. CROXALL & JAKACKI STACK -->
      <section>
        <section>
          <h1>Teaching as Invisible Labor</h1>
          <p>Croxall &amp; Jakacki, citing Katharine Harris</p>
        </section>
        <section>
          <blockquote>
            &ldquo;Teaching is often invisible labor, [even] though it&rsquo;s the largest part of our jobs. Teaching and learning are hard to see not because of their size but instead because the institutions that employ us have chosen to value research and its outputs (publications, chiefly) rather than pedagogy and its results (spread of knowledge, chiefly, and often enlarged and even changed minds). This undervaluing of educational effort continues despite the fact that it is the students, through their tuition dollars, who make possible the time faculty have to research.&rdquo;
            <span class="attribution">&mdash; Croxall &amp; Jakacki, citing Katharine Harris</span>
          </blockquote>
          <aside class="notes">
            Croxall, Brian and Diane K. Jakacki. "What We Teach When We Teach DH." https://dhdebates.gc.cuny.edu/read/what-we-teach-when-we-teach-dh/
          </aside>
        </section>
      </section>

      <!-- 11. LOCKE — FOUR PILLARS STACK -->
      <section>
        <section>
          <h1>DH Pedagogy as Essential Liberal Arts</h1>
          <p>Brandon Locke &mdash; four learning objectives</p>
        </section>
        <section>
          <blockquote>
            &ldquo;The framework employed in LEADR consists of four flexible learning objectives that can be packaged and built upon to meet the needs of the course and the desired outcomes of the faculty member and partners. The objectives are 1) Information Literacy, 2) Digital Literacy, 3) Data Literacy, and 4) Computational Analysis.&rdquo;
            <span class="attribution">&mdash; Locke, <em>DHQ</em> 11.3 (2017)</span>
          </blockquote>
          <aside class="notes">
            Locke, Brandon. "Digital Humanities Pedagogy as Essential Liberal Education." DHQ 11.3 (2017). https://www.digitalhumanities.org/dhq/vol/11/3/000303/000303.html
          </aside>
        </section>
      </section>

      <!-- 12. LOCKE — INFORMATION LITERACY STACK -->
      <section>
        <section>
          <h1>Information Literacy</h1>
          <p>ACRL Framework &mdash; six frames</p>
        </section>
        <section>
          <blockquote>
            &ldquo;An overarching set of abilities in which students are consumers and creators of information who can participate successfully in collaborative spaces.&rdquo;
            <span class="attribution">&mdash; ACRL 2015, via Locke</span>
          </blockquote>
          <ul>
            <li>Authority Is Constructed and Contextual</li>
            <li>Information Creation as a Process</li>
            <li>Information Has Value</li>
            <li>Research as Inquiry</li>
            <li>Scholarship as Conversation</li>
            <li>Searching as Strategic Exploration</li>
          </ul>
          <aside class="notes">
            ACRL Framework for Information Literacy for Higher Education (2015), six frames as cited in Locke 2017.
          </aside>
        </section>
      </section>

      <!-- 13. LOCKE — DIGITAL LITERACY STACK -->
      <section>
        <section>
          <h1>Digital Literacy</h1>
          <p>ALA definition &mdash; cognitive plus technical</p>
        </section>
        <section>
          <blockquote>
            &ldquo;The ability to use information and communication technologies to find, understand, evaluate, create, and communicate digital information, an ability that requires both cognitive and technical skills.&rdquo;
            <span class="attribution">&mdash; ALA 2013, via Locke</span>
          </blockquote>
          <p>Locke notes substantial overlap with information literacy, and that academic libraries play a key role &mdash; undergraduate students are not nearly as digitally literate as often assumed.</p>
          <aside class="notes">
            Locke 2017, citing ALA Digital Literacy report 2013, pp. 1, 5–17.
          </aside>
        </section>
      </section>

      <!-- 14. LOCKE — COMPUTATIONAL ANALYSIS STACK -->
      <section>
        <section>
          <h1>Computational Analysis</h1>
          <p>Methods grounded in disciplinary methodology</p>
        </section>
        <section>
          <blockquote>
            &ldquo;A wide-ranging set of tools and methodologies that rely upon computational processes to assist in the asking and exploring of research questions, including but not limited to text mining, network analysis, GIS and web mapping, 3D modeling, desktop fabrication, and topic modeling. These lessons are grounded in disciplinary methodology, and illustrate the ways in which scholars are using computationally-aided methods to ask new questions of their sources, as well as exploring more traditional questions.&rdquo;
            <span class="attribution">&mdash; Locke, <em>DHQ</em> 11.3 (2017)</span>
          </blockquote>
          <aside class="notes">
            Locke 2017, on computational analysis as a fourth literacy alongside information, digital, and data.
          </aside>
        </section>
      </section>

      <!-- 15. RUSHKOFF STACK -->
      <section>
        <section>
          <h1>Program or Be Programmed</h1>
          <p>Douglas Rushkoff &mdash; do you have to learn to program?</p>
        </section>
        <section>
          <blockquote>
            &ldquo;And while I always answer, &lsquo;Yes, you do have to learn to program,&rsquo; the real answer is probably no. You don&rsquo;t. You can get by without becoming a literate participant of the digital age. You may not know what&rsquo;s going on, you may not have much of an impact on the future of our species, and you may begin to feel like technology knows more about you than you know about it &mdash; but no, you don&rsquo;t have to learn to program.&rdquo;
            <span class="attribution">&mdash; Rushkoff &amp; Purvis, <em>Program or Be Programmed</em> (2011), 7&ndash;8</span>
          </blockquote>
          <aside class="notes">
            Rushkoff and Purvis 2011, pp. 7–8, as cited via Locke 2017.
          </aside>
        </section>
      </section>

      <!-- 16. IMAGE — Mollick AI policy -->
      <section class="webcomic-no">
        <h1>An AI Policy</h1>
        <img src="images/ai-policy.png" alt="Ethan Mollick's syllabus AI policy: students are expected to use AI as a writing/thinking tool, must take responsibility for accuracy and citation, and the AI is treated as a sometimes-unreliable collaborator." />
        <p class="image-caption">Ethan Mollick&rsquo;s class AI policy</p>
        <aside class="notes">
          Image from Mollick, "My class required AI." https://www.oneusefulthing.org/p/my-class-required-ai-heres-what-ive
        </aside>
      </section>

      <!-- 17. MOLLICK — "Without training" STACK -->
      <section>
        <section>
          <h1>Without Training, Everyone Uses AI Wrong</h1>
          <p>Ethan Mollick &mdash; on bad first prompts</p>
        </section>
        <section>
          <blockquote>
            &ldquo;I have been hearing reports from teachers about how they are seeing lots of badly-written AI essays, even though ChatGPT is capable of quite good writing. I think I know why. Almost everyone&rsquo;s initial attempts at using AI are bad. Almost everyone&rsquo;s first prompts were very straightforward. They usually pasted in the assignment directly, something like generate a 5 paragraph essay on selecting leaders. Sometimes they went a little further: use an academic tone or write it for an MBA class. The result was almost always a mediocre C&minus; essay. I think this is what most teachers are seeing, and why a lot of people underestimate what ChatGPT can do as a writing tool.&rdquo;
            <span class="attribution">&mdash; Mollick, <em>One Useful Thing</em></span>
          </blockquote>
          <aside class="notes">
            Mollick, "My class required AI. Here's what I've learned so far." https://www.oneusefulthing.org/p/my-class-required-ai-heres-what-ive
          </aside>
        </section>
      </section>

      <!-- 18. IMAGE — SMBC webcomic -->
      <section class="webcomic">
        <h1>Prompts, briefly</h1>
        <img src="images/smbc-prompt.png" alt="A Saturday Morning Breakfast Cereal webcomic about how prompts to AI tools shape the output." />
        <p class="image-caption">Zach Weinersmith, <em>SMBC</em></p>
        <aside class="notes">
          Webcomic credit: Saturday Morning Breakfast Cereal (smbc-comics.com). Used here to illustrate Mollick's point that initial prompts are usually bad.
        </aside>
      </section>

      <!-- 19. MOLLICK — "Students understand accuracy and bias" STACK -->
      <section>
        <section>
          <h1>Students Understand Accuracy and Bias Issues</h1>
          <p>Ethan Mollick &mdash; on hallucinations in student work</p>
        </section>
        <section>
          <blockquote>
            &ldquo;I have seen lots of educators concerned about the fact that the AI lies, frequently and well. But, seeing my students&rsquo; work, I think this is less of a problem than many think. Students understood the unreliability of AI very quickly, and took seriously my policy that they are responsible for the facts in their essays. It was clear that they carefully checked the assertions in the AI work (another learning opportunity!), and many reported finding the usual hallucinations &mdash; made up stories, made up citations &mdash; though the degree to which these problems were overt varied from prompt to prompt.&rdquo;
            <span class="attribution">&mdash; Mollick, <em>One Useful Thing</em></span>
          </blockquote>
          <aside class="notes">
            Mollick, "My class required AI. Here's what I've learned so far." https://www.oneusefulthing.org/p/my-class-required-ai-heres-what-ive
          </aside>
        </section>
      </section>

      <!-- 20. IMAGE — xkcd disclaimer -->
      <section class="webcomic">
        <h1>Disclaimers</h1>
        <img src="images/xkcd-disclaimer.png" alt="An xkcd webcomic about disclaimers and trust in AI-generated content." />
        <p class="image-caption">Randall Munroe, <em>xkcd</em></p>
        <aside class="notes">
          Webcomic credit: xkcd (xkcd.com). Paired with Mollick's point about students learning to verify AI-generated content.
        </aside>
      </section>

      <!-- 21. MOLLICK — "AI is everywhere already" STACK -->
      <section>
        <section>
          <h1>AI Is Everywhere Already</h1>
          <p>Ethan Mollick &mdash; the world of teaching is now more complicated</p>
        </section>
        <section>
          <blockquote>
            &ldquo;Even if I didn&rsquo;t embrace AI, it is also clear that AI is now everywhere in classes. For example, students used it to help them come up with ideas for class projects, even before I even taught them how to do that. As a result, the projects this semester are much better than previous pre-AI classes. This has led to greater project success rates and more engaged teams. On the downside, I find students also raise their hands to ask questions less. I suspect this might be because, as one of them told me, they can later ask ChatGPT to explain things they didn&rsquo;t get without needing to speak in front of the class. The world of teaching is now more complicated in ways that are exciting, as well as a bit unnerving.&rdquo;
            <span class="attribution">&mdash; Mollick, <em>One Useful Thing</em></span>
          </blockquote>
          <aside class="notes">
            Mollick, "My class required AI. Here's what I've learned so far." https://www.oneusefulthing.org/p/my-class-required-ai-heres-what-ive
          </aside>
        </section>
      </section>

      <!-- 22. CLOSING — Week 1 to-dos -->
      <section>
        <h1>This Week</h1>
        <ul>
          <li><strong>Activity Verification</strong> &mdash; due Friday, May 15.</li>
          <li><strong>Discussion: Introduce yourself</strong> &mdash; due Sunday, May 17.</li>
          <li><strong>Optional NEH Workshop 1</strong> &mdash; Tuesday, May 13, 10 AM&ndash;noon, CHDR.</li>
        </ul>
        <p>See <code>weeks/week-01.md</code> on Canvas for readings, links, and the discussion prompt.</p>
      </section>

    </div>
  </div>

  <!-- reveal.js 5.2.1 -->
  <script src="https://cdn.jsdelivr.net/npm/reveal.js@5.2.1/dist/reveal.js"></script>
  <script>
    // Initialize reveal.js
    Reveal.initialize({
      hash: true,
      slideNumber: 'c/t',
      controls: true,
      progress: true,
      center: false,
      transition: 'slide'
    });

    // In-deck dark-mode toggle. Uses the same localStorage key ("theme")
    // and same data-theme values ("light"/"dark") as the parent site,
    // so the deck and site stay in sync via shared origin localStorage.
    (function () {
      var html = document.documentElement;
      var btn = document.getElementById('deck-theme-toggle');
      var saved = null;
      try { saved = localStorage.getItem('theme'); } catch (e) { /* private mode */ }
      if (saved === 'light' || saved === 'dark') {
        html.setAttribute('data-theme', saved);
      } else if (window.matchMedia && window.matchMedia('(prefers-color-scheme: dark)').matches) {
        html.setAttribute('data-theme', 'dark');
      }
      if (btn) {
        btn.addEventListener('click', function () {
          var current = html.getAttribute('data-theme') || 'light';
          var next = current === 'dark' ? 'light' : 'dark';
          html.setAttribute('data-theme', next);
          try { localStorage.setItem('theme', next); } catch (e) { /* private mode */ }
        });
      }
    })();
  </script>
</body>
</html>
```

- [ ] **Step 2: Sanity check the file size and structure**

```
(Get-Item slides\week-01\index.html).Length
```

Expected: > 15000 bytes (the full deck content).

```
Select-String -Path slides\week-01\index.html -Pattern "<section" | Measure-Object -Line
```

Expected: roughly 30+ matches (one per `<section` opening tag, including nested vertical sections).

- [ ] **Step 3: Commit**

```
git add slides/week-01/index.html
git commit -m "feat: add Week 1 reveal.js slide deck"
```

---

## Task 8: Add `slides: week-01` to the Week 1 markdown

**Files:**
- Modify: `weeks/week-01.md`

- [ ] **Step 1: Add the frontmatter field**

Edit `weeks/week-01.md`. Find the first six frontmatter lines:

```yaml
---
week: 1
title: "Welcome and Interdisciplinary Teaching"
starts: 2026-05-11
workshop:
  title: "Workshop 1: Introducing AI for DH Pedagogy"
```

Replace with:

```yaml
---
week: 1
title: "Welcome and Interdisciplinary Teaching"
starts: 2026-05-11
slides: week-01
workshop:
  title: "Workshop 1: Introducing AI for DH Pedagogy"
```

(The body of the file is unchanged. The slides block will be spliced in by `canvas_sync` at push time, between the intro paragraph and the `## Readings` heading.)

- [ ] **Step 2: Verify with the parser**

```
python -c "from canvas_sync.parser import parse_week_file; w = parse_week_file('weeks/week-01.md'); print('slides=', repr(w.slides))"
```

Expected output:
```
slides= 'week-01'
```

- [ ] **Step 3: Commit**

```
git add weeks/week-01.md
git commit -m "feat: link Week 1 page to slide deck via slides frontmatter"
```

---

## Task 9: Verify locally and on GitHub Pages

This task uses no automation — it is the human-in-the-loop verification gate from the spec. Stop after Step 4. **Do not run `python -m canvas_sync push`** — the instructor will do that manually after confirming the deck looks right on the live URL.

- [ ] **Step 1: Run the full test suite one last time**

```
pytest -v
```

Expected: all tests PASS.

- [ ] **Step 2: Local browser preview of the deck**

Start the Jekyll server:

```
bundle exec jekyll serve
```

In a browser, open:

```
http://localhost:4000/InterdisciplinaryTeaching/slides/week-01/
```

Walk every slide:
- Right arrow steps through the 22 horizontal positions.
- Down arrow drops into vertical stacks (slides 10, 11, 12, 13, 14, 15, 17, 19, 21).
- Verify all three images render at full visible size without overflow.
- Verify webcomic slides (18, 20) have the accent border.
- Verify the title slide (1), section header (9), and closing slide (22) all render.
- Click the dark-mode toggle in the top-right; confirm the deck's colors swap and the choice persists on reload.
- Press `s` to open speaker view; confirm citations appear in the notes pane on quote slides.
- Open the browser DevTools console; confirm no errors (404s, mixed content, font-load failures).

If any slide overflows, has a broken image, or has a console error, fix the corresponding section in `slides/week-01/index.html` (or theme rule in `assets/css/reveal-theme.css`), reload, and retest.

- [ ] **Step 3: Local Canvas-page diff (no API write)**

In a separate terminal (without stopping `jekyll serve`):

```
python -m canvas_sync diff --week 1
```

Expected: a diff showing the page body now contains the slides iframe block (`<h2>Slides</h2>` + iframe + fallback link) inserted before the `<h2>Readings</h2>` heading. No other diffs should appear except those caused by genuine content edits to `weeks/week-01.md`.

If `canvas_config.yaml` is not configured locally, this command will fail. That's fine — record that the diff step needs to run on the instructor's configured machine.

- [ ] **Step 4: Push to GitHub Pages and verify the live URL**

```
git push origin main
```

Wait for the GitHub Pages build to complete (1&ndash;2 minutes). Then in a browser:

```
https://anastasiasalter.net/InterdisciplinaryTeaching/slides/week-01/
```

Open in an incognito/private window to rule out cache. Confirm:
- The deck loads and looks identical to the localhost preview.
- The three images load (relative `images/` paths resolve under the deployed path).
- No mixed-content warnings or font-load failures in DevTools.
- Dark-mode toggle works.

If anything is wrong, fix in source, commit, push, wait for the rebuild, and re-verify.

- [ ] **Step 5: STOP**

Do not push to Canvas. Report back to the instructor that the deck is live at the GitHub Pages URL and ready for `python -m canvas_sync push --week 1` at their discretion.

---

## Self-Review (writer's checklist)

**Spec coverage:**
- "self-contained reveal.js deck for Week 1" → Tasks 5–7.
- "visual coherence with the course site" → Task 6 (theme).
- "reusable mechanism — adding `slides: <slug>`" → Tasks 1–4 + Task 8.
- "no code changes that impact weeks without `slides:` set" → covered (defaults to `None`; splicer is no-op for empty input; pre-existing tests preserved).
- All 22 slide positions + 9 stacks → Task 7 contains all 31 sections.
- Speaker notes via `<aside class="notes">` → Task 7 quote and webcomic slides include them.
- Light/dark toggle with shared `localStorage` key `theme` → Task 7 init script + Task 6 CSS.
- Iframe template (16:9 padding-bottom, fallback link, inline styles) → Task 2 method body.
- Splice before first `<h2>`, append if absent → Task 4 function + tests.
- New parser/splicer test → Tasks 3 and 4.
- Verification workflow Steps 1–3, with Step 4/5 deferred → Task 9.

**Placeholder scan:** no TBD/TODO. Every code block is concrete; every test asserts specific values; every command is exact.

**Type/name consistency:**
- `Week.slides` (Optional[str]) — defined in Task 2, set in Task 3, read in Task 4.
- `Week.slides_section_html(base_url)` — defined in Task 2, called in Task 4.
- `Config.slides_base_url` — defined in Task 1, read in Task 4 via `cs.config.slides_base_url`.
- `_splice_slides(body_html, slides_html)` — defined and tested in Task 4.
- `CanvasSync.config` (public attr) — added in Task 4 Step 5, used in same task.

All names are consistent across tasks.

---

## Execution Handoff

Plan complete and saved to `docs/superpowers/plans/2026-05-08-week-01-slides.md`. Two execution options:

1. **Subagent-Driven (recommended)** — I dispatch a fresh subagent per task, review between tasks, fast iteration.
2. **Inline Execution** — Execute tasks in this session using `executing-plans`, batch execution with checkpoints.

Which approach?
